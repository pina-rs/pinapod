coverage:
  status:
    patch:
      default:
        # Compile-fail and generated-code contracts run in rustc subprocesses,
        # outside the source-level LLVM profile. Keep this gate demanding while
        # Miri, Kani, and trybuild enforce the safety-specific paths directly.
        target: 85%
